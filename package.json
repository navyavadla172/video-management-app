{
  "name": "video-management-app",
  "version": "1.0.0",
  "description": "A video management app with frontend and backend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\"",
    "install": "npm install",
    "install-frontend": "npm install --prefix frontend",
    "install-backend": "npm install --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.7"
  }
}
